# PulseBoard Android

Android Compose implementation of PulseBoard. It mirrors the HarmonyOS version with a home screen that supports dynamic and list styles, settings, status freshness indicators, and a detail screen for books, movies, places, and podcasts.

Architecture: `UI -> ViewModel/UI State -> UseCase -> Repository -> Mock data`.

Open the `android` directory in Android Studio and run the `app` configuration.
