# PulseBoard / 近况

一个使用 ArkTS + ArkUI 编写的 HarmonyOS Stage 模型示例。首页展示关注的人及更新鲜活度；详情页用紧凑的一屏布局汇总书、电影、旅行和播客。

## 运行

1. 使用 DevEco Studio 打开本目录。
2. 等待 ohpm / Hvigor 同步。
3. 选择 `entry` 模块，在 HarmonyOS 模拟器或真机运行。

Mock 数据位于 `entry/src/main/resources/rawfile/users.json`。更新状态以 10 天为界：10 天内为绿色，超过 10 天为灰色。

